<<<<<<< HEAD
name: Deploy to EKS

on:
  push:
    branches:
      - main

jobs:
  deploy:
=======
name: Build and Deploy to EKS

on:
  push:
    branches: [ "main" ]

jobs:
  build:
>>>>>>> e2305197e681fa0c77f941aeaed0dd7b572616c3
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
<<<<<<< HEAD
      uses: actions/checkout@v4

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ap-south-1

    - name: Update kubeconfig
      run: aws eks update-kubeconfig --name eks-cluster --region ap-south-1

    - name: Deploy to EKS
      run: |
        kubectl apply -f k8s/deployment.yml
        kubectl apply -f k8s/service.yml
=======
      uses: actions/checkout@v3

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v2
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}

    - name: Login to Amazon ECR
      run: |
        aws ecr get-login-password --region ap-south-1 \ | docker login --username AWS --password-stdin 274193347344.dkr.ecr.ap-south-1.amazonaws.com

    - name: Build Docker Image
      run: |
        docker build -t frontend-app .

    - name: Tag Image
      run: |
        docker tag frontend-app:latest $ECR_REPOSITORY:latest

    - name: Push Image to ECR
      run: |
        docker push $ECR_REPOSITORY:latest
>>>>>>> e2305197e681fa0c77f941aeaed0dd7b572616c3
